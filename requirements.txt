# Pinned to the versions that produced the published results; the exact
# environment of each run is recorded in results/w*/env.json.
numpy==2.5.2
pandas==3.0.5
pyarrow==25.0.1
scipy==1.18.0
matplotlib==3.11.1
scikit-learn==1.9.0
